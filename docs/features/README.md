# Feature specs

One permanent, git-tracked spec file per feature: `[feature-name]-spec.md`.

Write the spec before starting work, then point `docs/current-feature.md` at it.
Specs stay after the feature ships — they're the historical record.
